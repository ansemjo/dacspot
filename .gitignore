# ignore the buildroot directories but keep archives
#buildroot-*.tar.*
buildroot-*/
dl/

# secret values shouldn't be tracked
secrets.cfg

# neither should ssh keys in rootfs
rootfs/etc/ssh/ssh_host_*
